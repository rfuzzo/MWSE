return {
	type = "method",
	description = [[Sets the Info's text to the `component.description` if there is one. Sets the Info's displayed text to `self.text` otherwise.]],
	arguments = {
		{ name = "component", type = "mwseMCMComponent", optional = true }
	}
}
