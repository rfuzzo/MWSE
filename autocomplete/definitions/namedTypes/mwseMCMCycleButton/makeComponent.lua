return {
	type = "method",
	description = [[This method creates the cycle button's button UI element and stores it in `self.elements.button` and `self.mouseOvers`.]],
	arguments = {
		{ name = "parentBlock",	type = "tes3uiElement" }
	}
}
