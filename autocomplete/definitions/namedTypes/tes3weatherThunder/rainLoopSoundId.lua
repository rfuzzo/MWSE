return {
	type = "value",
	description = [[The weather's rain loop sound ID. This can be changed, and the sound will switch over when the weather is active]],
	readOnly = false,
	valuetype = "string",
}