return {
	type = "method",
	description = [[Changes the `abbreviateHeader` field of this logger.
	
This function does exactly the same thing as `log.abbreviateHeader = newAbbreviateHeader`. 
Use whichever one you prefer.
]],
	arguments = {
		{ name = "newAbbreviateHeader", type = "boolean" }
	}
}
