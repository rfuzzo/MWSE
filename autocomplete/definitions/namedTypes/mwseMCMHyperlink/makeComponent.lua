return {
	type = "method",
	description = [[This method creates the link (a text select) UI element of the Hyperlink and stores it in `self.elements.info`.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
