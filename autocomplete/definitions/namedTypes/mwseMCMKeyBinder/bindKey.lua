return {
	type = "method",
	description = [[Stores the value of the `currentCombo` to the variable if needed. Notifies the user of the result.]],
	arguments = {
		{ name = "e", type = "tes3messageBoxCallbackData", description = "Only changes the variable's value if \"Rebind\" button was pressed." }
	}
}
