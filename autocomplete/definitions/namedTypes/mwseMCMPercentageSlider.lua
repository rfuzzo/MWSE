return {
	type = "class",
	description = [[A slider that displays values as percentages in the range 0%-100%, while storing values in the range 0.0-1.0. The minimum and maximum range can be changed via the `min` and `max` parameters.]],
	inherits = "mwseMCMSlider"
}
