return {
	type = "method",
	description = [[Scales given `value` from the underlying `tes3uiSlider` widget's range to the range used by the variable.]],
	arguments = {
		{ name = "value", type = "number" }
	},
	returns = {{
		name = "scaledValue", type = "number"
	}}
}
