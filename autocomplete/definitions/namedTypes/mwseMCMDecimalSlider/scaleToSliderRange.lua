return {
	type = "method",
	description = [[Scales given `value` from the variable range to the range used by the underlying `tes3uiSlider` widget.]],
	arguments = {
		{ name = "value", type = "number" }
	},
	returns = {{
		name = "scaledValue", type = "number"
	}}
}
