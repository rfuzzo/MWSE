return {
	type = "method",
	description = [[Creates the sideToSideBlock UI element, and stores it in the `self.elements.sideToSideBlock`. The sideToSideBlock is the parent UI element for the left and right columns of the Sidebar Page.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
