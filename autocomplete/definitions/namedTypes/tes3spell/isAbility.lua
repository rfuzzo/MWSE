return {
	type = "value",
	description = [[`true` if the spell is an ability. This is equivalent to checking the `.castType` property against the constant.]],
	readOnly = true,
	valuetype = "boolean",
}