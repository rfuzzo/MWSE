return {
	type = "value",
	description = [[`true` if the spell is a common or blight disease. This is equivalent to checking the `.castType` property against the constants.]],
	readOnly = true,
	valuetype = "boolean",
}