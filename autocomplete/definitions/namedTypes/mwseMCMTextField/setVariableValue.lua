return {
	type = "method",
	description = [[Changes the TextField's `variable.value` to the given value, updates the TextField's label and widget, and calls `self:update`.]],
	arguments = {
		{ name = "newValue", type = "string" }
	}
}
