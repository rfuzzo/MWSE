return {
	type = "value",
	description = [[The text shown in a messageBox when the user entered an invalid input when `self.variable.numbersOnly` is true. The default text is a localized version of: "Value must be a number.".]],
	valuetype = "string",
}
