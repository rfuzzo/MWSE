return {
	type = "method",
	description = [[This method creates the submit button UI element and stores it in `self.elements.submitButton`.]],
	arguments = {
		{ name = "parentBlock",	type = "tes3uiElement" }
	}
}
