return {
	type = "value",
	description = [[*Optional*. *Default*: `false`. If true, updating the setting containing this variable will notify the player to restart the game.]],
	valuetype = "boolean?",
}
