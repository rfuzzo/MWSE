return {
	type = "method",
	description = [[Gets the parent table of the element's lua data, or `nil` if it has not been created. This function should not be called for getting or setting data. Instead, use `getLuaData` and `setLuaData`. This property exists for diagnostics and internal use.]],
	valuetype = "table?",
}
