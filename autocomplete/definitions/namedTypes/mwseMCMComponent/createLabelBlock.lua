return {
	type = "method",
	description = [[Creates component's labelBlock UI element inside given `parentBlock`, and stores it in the `self.elements.labelBlock` and `self.mouseOvers`.]],
	arguments = {
		{ name = "parentBlock",	type = "tes3uiElement" }
	}
}
