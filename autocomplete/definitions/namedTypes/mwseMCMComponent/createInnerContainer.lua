return {
	type = "method",
	description = [[Creates component's innerContainer UI element inside given `parentBlock`, and stores it in the `self.elements.innerContainer`. The innerContainer will add `self.indent` additional padding on the left if the component has a label.]],
	arguments = {
		{ name = "parentBlock",	type = "tes3uiElement" }
	}
}
