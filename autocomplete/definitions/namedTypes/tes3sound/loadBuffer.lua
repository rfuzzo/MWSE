return {
	type = "method",
	description = [[Loads the sound buffer. Does nothing if the sound buffer already exists.]],
	arguments = {
		{ name = "isPointSound", type = "boolean", optional = false, description = "If true, the sound is positional." },
	},
}
