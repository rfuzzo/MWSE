return {
	type = "method",
	description = [[Recursively walks over children of given `element` and inserts them into `self.mouseOvers`.]],
	arguments = {
		{ name = "element", type = "tes3uiElement" }
	}
}
