return {
	type = "method",
	description = [[This method creates the info (a label) UI element of the mwseMCMInfo and stores it in `self.elements.info` and `self.mouseOvers`.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
