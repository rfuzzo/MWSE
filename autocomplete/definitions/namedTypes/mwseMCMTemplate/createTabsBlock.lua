return {
	type = "method",
	description = [[This method creates the outerTabsBlock, previousTabButton, tabsBlock and nextTabButton UI elements of the Template. The page tabs aren't created if the Template contains only one page.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
