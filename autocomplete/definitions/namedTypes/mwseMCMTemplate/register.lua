return {
	type = "method",
	description = [[A convenience function that registers the mod's configuration menu using its Template. In addition, sets `self:onSearchInternal` as the `onSearch` handler.

You don't need to call `mwse.registerModConfig` or `mwse.mcm.register` if calling this function.]],
}
