return {
	type = "method",
	description = [[A convenience function that registers the mod's configuration menu using its Template.

You don't need to call `mwse.registerModConfig` or `mwse.mcm.register` if calling this function.]],
}
