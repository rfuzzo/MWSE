return {
	type = "value",
	description = [[If true, when the user searches the MCM list, all the pages and settings in this MCM template will be searched over. The matching will be performed on setting `description` fields.]],
	valuetype = "boolean",
}
