return {
	type = "method",
	description = [[If a Template has headerImagePath defined, this method creates an image UI element from image at given path. If not, creates a label UI block with `self.label` text.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
