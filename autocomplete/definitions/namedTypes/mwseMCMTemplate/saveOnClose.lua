return {
	type = "method",
	description = [[This method assigns a new function to `onClose` field that calls `mwse.saveConfig` with given parameters.]],
	arguments = {
		{ name = "fileName", type = "string" },
		{ name = "config", type = "unknown" },
	}
}
