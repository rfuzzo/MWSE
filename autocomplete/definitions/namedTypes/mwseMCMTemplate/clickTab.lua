return {
	type = "method",
	description = [[Destroys the currently display page and creates the given page as the new current page.]],
	arguments = {
		{ name = "thisPage", type = "mwseMCMPage" }
	}
}
