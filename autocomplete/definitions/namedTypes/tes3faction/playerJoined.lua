return {
	type = "value",
	description = [[The player's join state for the faction. You should prefer to use the `join` or `leave` functions to modify this, to ensure the changes are visible to all systems.]],
	valuetype = "boolean",
}