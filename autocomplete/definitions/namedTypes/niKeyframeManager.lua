return {
	type = "class",
	description = [[A controller class responsible for controlling the animation sequences stored in the KF file. It controls the animations, and allows to smoothly transition between two animations by blend, morph, and sum transitions.]],
	inherits = "niTimeController",
}
