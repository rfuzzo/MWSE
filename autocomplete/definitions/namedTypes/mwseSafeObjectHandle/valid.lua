return {
	type = "method",
	description = [[Returns true if the object of this handle is still valid. Invalid objects no longer exist or were deleted.]],
	returns = {
		{ name = "valid", type = "boolean" }
	}
}