return {
	type = "method",
	description = [[Creates a thin border UI element as the left column of the Filter Page and stores it in `self.elements.outerContainer`.]],
	arguments = {
		{ name = "parentBlock",	type = "tes3uiElement" }
	}
}
