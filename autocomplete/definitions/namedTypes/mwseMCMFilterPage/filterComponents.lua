return {
	type = "method",
	description = [[This method will hide all the UI elements of components in `self.components` if their label doesn't contain the text currently inputted in the search bar.]],
}
