return {
	type = "method",
	description = [[Creates a label UI element with `self.description` text and adds it to `self.elements.description`.]],
	arguments = {
		{ name = "parentBlock",	type = "tes3uiElement" }
	}
}
