return {
	type = "value",
	description = [[This allows indexing a local script variable by it's name. The variable's value can be read or modified this way. There is an example at the top of the page on doing this.]],
	readOnly = true,
	valuetype = "number|nil",
}
