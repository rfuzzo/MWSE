return {
	type = "value",
	description = [[This function is called when the value of the variable is changed. The function can modify the new value before it is stored.]],
	valuetype = "fun(newValue): unknown",
}
