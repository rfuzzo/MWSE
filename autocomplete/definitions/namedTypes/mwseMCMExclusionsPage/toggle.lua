return {
	type = "method",
	description = [[Toggles the `e.source` UI element from one list to the other. Updates the variable value accordingly.]],
	arguments = {
		{ name = "e", type = "tes3uiEventData" }
	}
}
