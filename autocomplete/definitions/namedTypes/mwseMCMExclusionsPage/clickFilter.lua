return {
	type = "method",
	description = [[This method sets the state of clicked filter button to `tes3.uiState.active`, and all the other filter buttons to `tes3.uiState.normal`.]],
	arguments = {
		{ name = "filter", type = "tes3uiElement" }
	}
}
