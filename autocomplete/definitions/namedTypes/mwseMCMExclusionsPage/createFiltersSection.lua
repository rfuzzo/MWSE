return {
	type = "method",
	description = [[Creates a button for each of the filters as children of `filterList` UI element. If the Cxclusions Page has only one filter, this section won't be visible.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
