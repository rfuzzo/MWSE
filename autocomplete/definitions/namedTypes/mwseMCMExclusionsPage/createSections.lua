return {
	type = "method",
	description = [[This method creates the `sections` UI element and stores it in `self.elements.sections`.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
