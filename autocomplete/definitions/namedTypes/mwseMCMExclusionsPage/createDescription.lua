return {
	type = "method",
	description = [[If the `description` for this page is set, this method will create a label with the `description` as text and add it to `self.elements.description`.]],
	arguments = {
		{ name = "parentBlock", type = "tes3uiElement" }
	}
}
