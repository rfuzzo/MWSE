return {
	type = "value",
	description = [[Fixes the height of the paragraph field to a custom value. The height is set on the `self.elements.scrollPane.parent.height` UI element.]],
	valuetype = "integer|nil",
}
