return {
	type = "method",
	description = [[This method an event handler on tes3.uiEvent.keyEnter that calls `self:update()` if the pressed key was ++enter++. Inserts a newline into the paragraph input if the pressed combination was ++shift+enter++.]],
	arguments = {
		{ name = "element",	type = "tes3uiElement" }
	}
}
