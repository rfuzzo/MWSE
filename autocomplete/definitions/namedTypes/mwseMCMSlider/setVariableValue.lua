return {
	type = "method",
	description = [[Changes the Slider's `variable.value` to the given value, updates the Slider's label and widget, and calls `self:update`.]],
	arguments = {
		{ name = "newValue", type = "number" }
	}
}
