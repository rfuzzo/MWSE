return {
	type = "method",
	description = [[Reads the current value of the underlying `tes3uiSlider` widget and scales it to the value range used by the variable.]],
	returns = {{
		name = "value", type = "number"
	}}
}
