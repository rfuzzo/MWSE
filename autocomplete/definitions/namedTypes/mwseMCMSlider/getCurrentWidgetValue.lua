return {
	type = "method",
	description = [[Reads the current variable value and scales it to the value range used by the underlying `tes3uiSlider` widget.]],
	returns = {{
		name = "value", type = "integer"
	}}
}
