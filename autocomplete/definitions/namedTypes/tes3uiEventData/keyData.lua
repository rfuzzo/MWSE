return {
	type = "value",
	readOnly = true,
	description = [[For `tes3.uiEvent.keyPress` events, this is the raw DirectInput data associated with the input. Note that the `keyCode` provided here maps to DirectInput `tes3.keyboardCode` enums, and are not the virtual scan codes from `tes3.scanCode`.]],
	valuetype = "mwseKeyCombo|nil",
}