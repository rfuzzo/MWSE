return {
	type = "value",
	readOnly = true,
	description = [[For `tes3.uiEvent.keyPress` events, this is the human-facing character that was pressed. While the `keyData` shows the direct keyboard state, `character` shows the translated input. Note that this is language-specific.]],
	valuetype = "string|nil",
}