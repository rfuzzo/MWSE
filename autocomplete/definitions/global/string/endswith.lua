return {
	type = "function",
	description = [[Returns `true` if a `string` ends with a given substring.
This function **does not** use pattern matching.
]],
	arguments = {
		{ name = "s", type = "string" },
		{ name = "pattern", type = "string" },
	},
	valuetype = "boolean",
}