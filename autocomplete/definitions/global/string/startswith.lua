return {
	type = "function",
	description = [[Returns `true` if a `string` begins with a given substring.
	Note: this function does not use pattern matching.]],
	arguments = {
		{ name = "s", type = "string" },
		{ name = "substring", type = "string" },
	},
	valuetype = "boolean",
}