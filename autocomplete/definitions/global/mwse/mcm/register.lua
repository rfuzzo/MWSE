return {
	type = "function",
	description = [[A convenience function that registers the mod's configuration menu using its mwseMCMTemplate.

You don't need to call `mwse.registerModConfig` if calling this function.]],
	arguments = {
		{ name = "template", type = "mwseMCMTemplate" },
	},
}
